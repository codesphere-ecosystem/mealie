# Stretch goal (Module 6, article 6.6): a "prod" CI profile variant for Keycloak,
# backed by a Managed Postgres instead of the embedded H2 database — the same
# CI-profile mechanism you're already using to keep Mealie and Keycloak separate
# (article 3.6, CI Profiles & Headless Services).
#
# Layered on top of values.yaml via: --values values.yaml --values values.prod.yaml
#
# Only attempt this once the dev-mode Keycloak (values.yaml, ci.keycloak.yml) is
# working end to end — this is additive, not a prerequisite for your submission.
#
# TODO: declare a `postgres` Managed Service in a "prod" CI profile of
# ci.keycloak.yml (provider: postgres v1, a plan, config.userName/databaseName,
# secrets from vault — see article 4.2, Integrating Managed Services in a
# Landscape, and article 4.3 for secret wiring), then point this file at it:
#
# database:
#   vendor: postgres
#   port: 5432
#   database: keycloak
#   username: keycloak
#
# dbchecker:
#   enabled: true
#
# The database hostname and password are passed in via --set/--set-string in the
# Helm command (not hardcoded here) — see the hostname pattern in article 4.2
# (Service Discovery & Environment Variables).
